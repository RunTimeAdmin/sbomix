# Test output / stray scan artifacts (never ship generated reports)
*.txt
bom.*.json
aibom.json
cra-report.json
agent-trust-report.json
agent-trust-report.html

# Development / CI — not needed by npm consumers
.env
.env.*
.eslintrc.json
.github/
docker-compose.yml
Dockerfile
scripts/
tests/
# Server-side files (not part of the CLI package)
src/api/

# Assets (images, not needed at runtime)
assets/

# Internal agent instructions — not for end users
CLAUDE.md

# Stray analysis/review docs
perf_check.js
perf_check.json

# Deployment scripts
deploy/

# OS / editor
.DS_Store
Thumbs.db
*.log
