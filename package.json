{
  "name": "packrai",
  "version": "0.1.0",
  "description": "Fast, accurate SBOM generation from GitHub repos and local lock files",
  "main": "src/pipeline.js",
  "bin": {
    "packrai": "./bin/packrai.js"
  },
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "lint": "eslint src/ bin/"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "smol-toml": "^1.3.1",
    "yaml": "^2.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["sbom", "cyclonedx", "spdx", "supply-chain", "security"],
  "license": "MIT"
}
